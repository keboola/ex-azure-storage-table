{
  "parameters": {
    "db": {
      "#connectionString": "%env(string:CONNECTION_STRING)%"
    },
    "table": "simple",
    "output": "output",
    "mode": "raw",
    "select": "PartitionKey, RowKey, name, age"
  }
}
